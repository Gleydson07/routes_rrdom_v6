import styled from 'styled-components';

export const Container = styled.div`
  width: fit-content;

  p{
    font-weight: 700;
  }
  
  input {
    display: block;
    margin: 8px 0;
    padding: 4px;
  }

  button {
    cursor: pointer;
  }
`;